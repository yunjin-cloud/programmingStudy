#include<stdio.h>

#define SIZE 10

typedef int element;
typedef struct
{
	int front;
	int rear;
	element data[SIZE];
}queue;

void init(queue* q)
{
	
}
int is_full(queue* q)
{
	
}
int is_empty(queue* q)
{
	
}
void enqueue(queue* q, element* v)
{
	
}
element dequeue(queue* q)
{
	
}
peek(queue* q)













